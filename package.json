{
  "name": "@oursky/stylelint-oursky",
  "version": "1.0.1",
  "main": "src/index.js",
  "files": [
    "src/index.js",
    "config"
  ],
  "scripts": {
    "test": "jest",
    "format": "prettier --write --list-different 'src/**/*.{js,jsx,ts,tsx}' 'config/**/*.{js,jsx,ts,tsx}'"
  },
  "dependencies": {
    "postcss": "^7.0.31",
    "stylelint-no-unsupported-browser-features": "4.0.0"
  },
  "peerDependencies": {
    "stylelint": "^13.0.0"
  },
  "devDependencies": {
    "jest": "^26.0.0",
    "prettier": "2.0.5",
    "stylelint": "^13.0.0"
  }
}
